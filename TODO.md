# Is the setup section of the readme correct ? Are there no missing packages / libraries that are actually required to install? (is csv builtin or need to install separately)

# NEW TABLES

I want two new tables. RIght now you have columns for TOP-500, TOP-1k, TOP-5k, TOP-12K
zeromissing words.
I want to have a table with the same columns but instead of having zero-missing, it should be N<=3.
Create scripts to generate these tables.

# NEW CROSS DATA SET ANALYSIS

# Refactors

# Make sure all scripts are working (rerun them again)

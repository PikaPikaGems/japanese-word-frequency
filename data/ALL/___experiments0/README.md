# Old Experiments (Pre-dataset Update)

These are old experiments from before more datasets were added to `consolidated.csv` and `categorized.csv`.

New experiments based on the updated datasets live in `___experiments1/`.
